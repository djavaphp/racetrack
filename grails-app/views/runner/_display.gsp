<div class="entry">

  <g:if test="${runnerInstance.filename}">
    <p>
      <img src="${createLinkTo(dir:'/krina/images.jpeg'}"
           alt="${runnerInstance.filename}"
           title="${runnerInstance.filename}" />
    </p>
  </g:if>
</div>